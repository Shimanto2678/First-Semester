#include <stdio.h>

int main()
{
   int one,two;

   printf("Enter the first number : ");
   scanf("%d",&one);
   printf("Enter the second number : ");
   scanf("%d",&two);
   int product = one + two;

   printf("The product of two integer is : %d",product);


    return 0;
}